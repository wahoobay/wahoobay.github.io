import React from 'react';
import Hint from '../Hint';
import Input from '../Input';
import withClassNames from '../../behaviors/classNames';
const TypeaheadInputSingle = ({ inputRef, referenceElementRef, ...props }) => (React.createElement(Hint, null,
    React.createElement(Input, { ...props, ref: (node) => {
            inputRef(node);
            referenceElementRef(node);
        } })));
export default withClassNames(TypeaheadInputSingle);
