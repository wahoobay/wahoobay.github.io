import { ComponentType } from 'react';
import type { Size } from '../types';
declare type Props = any & {
    className?: string;
    isInvalid?: boolean;
    isValid?: boolean;
    size?: Size;
};
declare function withClassNames(Component: ComponentType<Props>): {
    ({ className, isInvalid, isValid, size, ...props }: Props): JSX.Element;
    displayName: string;
};
export default withClassNames;
