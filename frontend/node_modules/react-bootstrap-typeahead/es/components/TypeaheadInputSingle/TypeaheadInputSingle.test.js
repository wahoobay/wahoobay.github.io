import React from 'react';
import * as stories from './TypeaheadInputSingle.stories';
import { composeStories, generateSnapshots, getInput, render, userEvent } from '../../tests/helpers';

var _composeStories = composeStories(stories),
    Disabled = _composeStories.Disabled;

describe('<TypeaheadInputSingle>', function () {
  generateSnapshots(stories);
  it('does not focus a disabled input', function () {
    render( /*#__PURE__*/React.createElement(Disabled, null));
    var input = getInput();
    expect(input).toBeDisabled();
    userEvent.click(input);
    expect(input).not.toHaveFocus();
  });
});