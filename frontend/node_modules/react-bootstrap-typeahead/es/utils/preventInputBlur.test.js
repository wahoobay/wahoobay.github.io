import preventInputBlur from './preventInputBlur';
describe('preventInputBlur', function () {
  it('calls `preventDefault` on the event', function () {
    var e = {
      preventDefault: jest.fn()
    };
    preventInputBlur(e);
    expect(e.preventDefault).toHaveBeenCalled();
  });
});