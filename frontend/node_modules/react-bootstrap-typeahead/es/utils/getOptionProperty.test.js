import getOptionProperty from './getOptionProperty';
describe('getOptionProperty', function () {
  it('retrieves the property from the option', function () {
    expect(getOptionProperty({
      foo: 'bar'
    }, 'foo')).toBe('bar');
    expect(getOptionProperty({}, 'foo')).toBeUndefined();
  });
});